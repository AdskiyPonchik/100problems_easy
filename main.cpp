#include "contains_duplicate/contains_duplicate.h"
#include "other_tasks/faktorial.h"

int main()
{
    //std::vector<int> arr{1, 1, 1, 3, 3, 4, 3, 2, 4, 2};
    //std::cout << containsDuplicate(arr) << '\n';
    int number = 5;
    std::cout << factorial(number) << '\n';
    return 0;
}